{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window capability. Sidecar is spawned from Rust via std::process (no shell plugin). App commands such as get_sidecar_base are allowed by default for bundled webviews.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "fs:default"
  ]
}
